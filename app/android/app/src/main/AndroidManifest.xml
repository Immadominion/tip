<manifest xmlns:android="http://schemas.android.com/apk/res/android">
    <!--
      Backup is off, on every path Android offers.

      allowBackup defaults to true, which sends the app's data to Google Drive
      and to device-to-device transfer. Two things make that wrong here.

      The Supabase session and its PKCE verifier sit in ordinary
      SharedPreferences, so they would be backed up in the clear and restored
      onto whatever device the backup lands on.

      And the seed's own storage is Keystore-backed, with a key that never
      leaves the device it was made on. Restoring the ciphertext onto a
      different device produces a decrypt failure against a key that is not
      there — which is the exact scenario that used to make the plugin delete
      the seed and drop the user into onboarding. That default is now off, but a
      backup that can only ever restore unreadable data is still not a backup.

      The real one is the encrypted recovery-phrase envelope, which the user
      makes deliberately with a password only they know.

      allowBackup covers Android 11 and below; dataExtractionRules covers 12 and
      up, where cloud backup and device transfer are decided separately.
    -->
    <application
        android:label="tip"
        android:name="${applicationName}"
        android:allowBackup="false"
        android:fullBackupContent="false"
        android:dataExtractionRules="@xml/data_extraction_rules"
        android:icon="@mipmap/ic_launcher">
        <activity
            android:name=".MainActivity"
            android:exported="true"
            android:launchMode="singleTop"
            android:taskAffinity=""
            android:theme="@style/LaunchTheme"
            android:configChanges="orientation|keyboardHidden|keyboard|screenSize|smallestScreenSize|locale|layoutDirection|fontScale|screenLayout|density|uiMode"
            android:hardwareAccelerated="true"
            android:windowSoftInputMode="adjustResize">
            <!-- Specifies an Android theme to apply to this Activity as soon as
                 the Android process has started. This theme is visible to the user
                 while the Flutter UI initializes. After that, this theme continues
                 to determine the Window background behind the Flutter UI. -->
            <meta-data
              android:name="io.flutter.embedding.android.NormalTheme"
              android:resource="@style/NormalTheme"
              />
            <intent-filter>
                <action android:name="android.intent.action.MAIN"/>
                <category android:name="android.intent.category.LAUNCHER"/>
            </intent-filter>

            <!-- tip://claim#<code>. Works with nothing hosted anywhere, which
                 is why the app answers to it at all. -->
            <intent-filter>
                <action android:name="android.intent.action.VIEW"/>
                <category android:name="android.intent.category.DEFAULT"/>
                <category android:name="android.intent.category.BROWSABLE"/>
                <data android:scheme="tip"/>
            </intent-filter>

            <!-- https://usetip.xyz/claim#<code>. autoVerify only succeeds once
                 the domain serves site/.well-known/assetlinks.json with this
                 build's signing fingerprint. Until then the filter is simply
                 not verified, which costs nothing. -->
            <intent-filter android:autoVerify="true">
                <action android:name="android.intent.action.VIEW"/>
                <category android:name="android.intent.category.DEFAULT"/>
                <category android:name="android.intent.category.BROWSABLE"/>
                <data
                    android:scheme="https"
                    android:host="usetip.xyz"
                    android:pathPrefix="/claim"/>
            </intent-filter>
        </activity>
        <!-- Don't delete the meta-data below.
             This is used by the Flutter tool to generate GeneratedPluginRegistrant.java -->
        <meta-data
            android:name="flutterEmbedding"
            android:value="2" />
    </application>
    <!-- Required to query activities that can process text, see:
         https://developer.android.com/training/package-visibility and
         https://developer.android.com/reference/android/content/Intent#ACTION_PROCESS_TEXT.

         In particular, this is used by the Flutter engine in io.flutter.plugin.text.ProcessTextPlugin. -->
    <queries>
        <intent>
            <action android:name="android.intent.action.PROCESS_TEXT"/>
            <data android:mimeType="text/plain"/>
        </intent>
    </queries>
</manifest>
